const message = () => {
  const dogBreed = "Huskimo"
  const dogName = "Emma";
  const dogsAge = 4;
  return dogName + ' is a ' + dogBreed + ' and is ' + dogsAge + ' years old. ';
};

export default message;
